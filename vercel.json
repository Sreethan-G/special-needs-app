{
  "version": 2,
  "builds": [
    {
      "src": "special-needs/package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install --legacy-peer-deps",
        "buildCommand": "expo export --platform web",
        "distDir": "special-needs/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/special-needs/dist/$1"
    }
  ]
}
